Iniciar loop

crear <ALGO ESTA HECHO EL OBJETO QUE ES LA BASE>
	donde los algos son:
		- objeto <done>
		- variable (en realidad es agregar variable a un objeto)
			*coleccion <done>
		- mensaje
		- clonar <done>
		
modificar un objeto
	agregar atributo (crear atributo dentro del objeto)
	modificar nombre
	agregar un metodo (crear un mensaje)
		/*si tuviesemos informacion de tipos es mucho mas inteligente todo el autocompletar y toda la sugerencia q me da*/
		cuando escribo un metodo que se valide:
			(cuando lo termino de escribir) si la variable no esta que me ofrezca crearla 
				como variable del objeto
				como variable temporal
			--(cuando lo termino de escribir) si un selector que uso no esta me deja guardar y no pregunta nada. Hoy en dia manda pop up, no sabemos si sacarlo o dejarlo es facil, no lo especificamos queda en el limbo y si es facil lo cambiamos sino saraza
			(todo el tiempo) autocomplete 
				selectores
				variables (objeto, locales y parametros)
		styling (colorcitos)
	

Desreferenciar objetos
	- no se borran objetos se DESREFERENCIAN (Guille o German lo especifican y lo desarrollan)

colecciones
	crear coleccion (clonar el set) 
	agregar coleccion a un objeto ya creado <done>
	
escribir codigo en el workspace	

